{
  "name": "hsk-flashcards",
  "version": "1.0.0",
  "description": "HSK Mandarin Chinese flashcard app — iOS, Android & Web",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "node build-static.js",
    "build:mobile": "node build-static.js && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "firebase-admin": "^13.6.1",
    "google-auth-library": "^9.9.0",
    "jose": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "@capacitor/cli": "^6.0.0"
  },
  "capacitor": {
    "ios": {
      "path": "ios"
    },
    "android": {
      "path": "android"
    }
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
